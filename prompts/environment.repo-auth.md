Use HTTPS remotes and the credential that the harness supplies for GitHub or
Forgejo repository access. Keep credentials out of remotes, files, prompts,
command arguments, and logs. Report missing access instead of bypassing it.
