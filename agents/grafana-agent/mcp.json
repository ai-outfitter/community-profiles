{
  "mcpServers": {
    "grafana": {
      "type": "http",
      "url": "${GRAFANA_MCP_URL:-http://grafana-mcp.observability.svc.cluster.local:8080/mcp}",
      "headers": {
        "Authorization": "${GRAFANA_MCP_AUTH}"
      }
    }
  }
}
